# Subagent 1 Tasks
- [ ] 1. Setup Flutter Riverpod architecture, theme, GoRouter in `lib/core/` and `lib/main.dart`.
- [ ] 2. Implement `HomeScreen` and `ChildProfileEntry` UI.
- [ ] 3. Setup `ConsentScreen` (just_audio) and `ElicitationScreen` cards and timers.
- [ ] 4. Setup `ProcessingScreen` and `ResultScreen` layouts.
- [ ] 5. Setup Next.js Dashboard scaffolding in `dashboard/` directory.

# Subagent 2 Tasks
- [x] 1. Update `supabase/schema.sql` with the Auth strategy (workers) and screenings table.
- [x] 2. Update Android project dependencies in `android/app/build.gradle.kts` for ONNX and other necessary libraries.
- [x] 3. Scaffold `UnprocessedAudioRecorder.kt`, `RollingBufferProcessor.kt`, `WebRTCVadBridge.kt`, `PyannoteRunner.kt`, and `FeatureExtractor.kt` inside `android/app/src/main/kotlin/com/mozhimuthal/mozhimuthal/`.
- [x] 4. Scaffold the Method channel wiring in Kotlin in `MainActivity.kt`.
